<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.6</version>
		<relativePath/>
	</parent>
	<groupId>com.example</groupId>
	<artifactId>file_translator</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<properties>
		<java.version>21</java.version>
		<springdoc.version>3.0.0</springdoc.version>
		<bucket4j.version>8.14.0</bucket4j.version>
		<awssdk.version>2.53.1</awssdk.version>
		<jacoco.version>0.8.13</jacoco.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<!-- Wypełniane przez jacoco:prepare-agent; pusta wartość domyślna sprawia,
		     że Surefire działa też wtedy, gdy JaCoCo nie zostało uruchomione. -->
		<surefireArgLine/>
	</properties>
	<!--
		BOM AWS SDK v2. Wersji tych artefaktów NIE zarządza BOM Spring Boota (to nie jest
		biblioteka z jego ekosystemu), więc bez tego importu każdą trzeba by podawać z osobna
		i pilnować, żeby się nie rozjechały - a SDK jest wielomodułowy i moduły muszą być
		w tej samej wersji.
	-->
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>software.amazon.awssdk</groupId>
				<artifactId>bom</artifactId>
				<version>${awssdk.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<!-- Mail and template engine for transactional emails -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>
		<!-- Klient HTTP do API dostawcy tłumaczeń (DeepL).
		     UWAGA, ta sama pułapka co przy liquibase: sama klasa RestClient siedzi
		     w spring-web, które jest już na ścieżce klas, ale wstrzykiwalny
		     RestClient.Builder oraz ClientHttpRequestFactoryBuilder (potrzebny do
		     USTAWIENIA TIMEOUTÓW) przyszły w Spring Boocie 4 do osobnego modułu.
		     Bez tej zależności kod się nie kompiluje, a bez timeoutów zawieszony
		     dostawca zatrzymałby kolejkę tłumaczeń bez śladu w logach. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-restclient</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-thymeleaf</artifactId>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- MockMvc i @AutoConfigureMockMvc. W Boot 4 wydzielone ze spring-boot-starter-test
		     do osobnego startera dla webmvc. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- JWT library -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.12.6</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.12.6</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.12.6</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>
		<!--
			Testcontainers - silniki produkcyjne (PostgreSQL, Redis, MinIO) podnoszone
			na czas testów. Wersją zarządza BOM Spring Boota (testcontainers-bom),
			więc bez podawania jej tutaj.

			UWAGA na nazwy artefaktów: w Testcontainers 2.x moduły nazywają się
			testcontainers-<moduł>, a nie samą nazwą silnika jak w 1.x - stary zapis
			<artifactId>postgresql</artifactId> rozwiązałby się na STEROWNIK JDBC
			PostgreSQL-a o tym samym identyfikatorze, a nie na moduł Testcontainers.

			Redis leci na GenericContainerze z rdzenia, świadomie bez modułu com.redis:
			potrzebny jest jeden wystawiony port, a nie API opakowujące go w klasę.
		-->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<!--
			Launcher JUnit Platform. Surefire dokłada go sobie sam DO URUCHOMIENIA, ale nie
			do KOMPILACJI testów - a TestInfrastructure implementuje LauncherSessionListener,
			czyli typ z tego artefaktu. Bez tej zależności kod się nie kompiluje, mimo że
			w czasie działania byłby na ścieżce klas. Wersją zarządza BOM Spring Boota.
		-->
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-launcher</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-minio</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Migracje bazy danych.
		     UWAGA: w Spring Boot 4 autokonfiguracje zostały wyniesione z spring-boot-autoconfigure
		     do osobnych modułów. Sam liquibase-core NIE uruchamia migracji - potrzebny jest
		     dodatkowo moduł spring-boot-liquibase z LiquibaseAutoConfiguration. -->
		<dependency>
			<groupId>org.liquibase</groupId>
			<artifactId>liquibase-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-liquibase</artifactId>
		</dependency>

		<!-- Health checki, metryki, endpointy operacyjne -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Ograniczanie liczby żądań (token bucket). Wariant _jdk17 to build
		     skompilowany pod nowsze JDK, z tym samym API co bucket4j-core. -->
		<dependency>
			<groupId>com.bucket4j</groupId>
			<artifactId>bucket4j_jdk17-core</artifactId>
			<version>${bucket4j.version}</version>
		</dependency>
		<!-- Cache z ograniczonym rozmiarem i TTL - przechowuje kubełki limitów w pamięci.
		     Zwykła ConcurrentHashMap rosłaby w nieskończoność przy ataku z wielu adresów IP. -->
		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
		</dependency>
		<!-- Rozproszony magazyn limitów (opcjonalny, włączany przez app.rate-limit.store=redis).
		     Ciągnie za sobą klienta lettuce-core - świadomie BEZ spring-boot-starter-data-redis,
		     żeby Redis nie stał się zależnością startu aplikacji, dopóki nie jest używany. -->
		<dependency>
			<groupId>com.bucket4j</groupId>
			<artifactId>bucket4j_jdk17-lettuce</artifactId>
			<version>${bucket4j.version}</version>
		</dependency>
		<!-- Klient Redisa. Wersją zarządza BOM Spring Boota, więc bez podawania jej tutaj. -->
		<dependency>
			<groupId>io.lettuce</groupId>
			<artifactId>lettuce-core</artifactId>
		</dependency>

		<!--
			Magazyn obiektowy na pliki użytkowników (S3, a lokalnie MinIO).

			AWS SDK v2 WPROST, bez Spring Cloud AWS: to jedna zależność zamiast BOM-u
			o własnym cyklu wydań, który przy Spring Boocie 4 (rozbite autokonfiguracje,
			Jackson 3) byłby kolejną powierzchnią niespodzianek. S3Client budujemy jawnym
			@Bean-em, więc widać wprost, skąd bierze się endpoint i poświadczenia.

			url-connection-client zamiast domyślnego klienta Apache: wystarcza w zupełności
			(żądania idą z puli wątków workera, nie asynchronicznie), a wyklucza z drzewa
			zależności całe apache-httpclient. Wykluczenia poniżej są konieczne - bez nich
			SDK znajduje na ścieżce klas DWA klienty synchroniczne i odmawia startu
			komunikatem "Multiple HTTP implementations were found".
		-->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>s3</artifactId>
			<exclusions>
				<exclusion>
					<groupId>software.amazon.awssdk</groupId>
					<artifactId>apache-client</artifactId>
				</exclusion>
				<exclusion>
					<groupId>software.amazon.awssdk</groupId>
					<artifactId>netty-nio-client</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>url-connection-client</artifactId>
		</dependency>

		<!-- Dokumentacja API generowana z kodu (OpenAPI 3 + Swagger UI) -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- Rozwiązuje wersję/ścieżkę do jara mockito-core na property,
			     żeby móc podać ją jako -javaagent w Surefire poniżej -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<id>getClasspathFilenames</id>
						<goals>
							<goal>properties</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<!-- @{surefireArgLine}: agent JaCoCo. Musi być tutaj, bo jawne <argLine>
					     nadpisuje to, co JaCoCo wstrzykuje domyślnie - klasyczna przyczyna
					     raportów pokrycia pokazujących 0%.
					     Składnia @{...}, a nie ${...}: Surefire rozwija @{...} dopiero
					     w momencie uruchomienia testów, czyli po tym, jak jacoco:prepare-agent
					     ustawi tę właściwość. ${...} zostałoby podstawione już przy wczytywaniu
					     POM-a - wtedy pustą wartością domyślną, i agent nigdy by nie wystartował.
					     -javaagent: rejestruje Mockito jako agenta JVM od razu przy starcie,
					     zamiast pozwalać mu dołączać się dynamicznie w locie (co JDK
					     będzie blokować w przyszłych wersjach - stąd ostrzeżenie w logach).
					     -Dfile.encoding=UTF-8: naprawia zniekształcone polskie znaki w logach na Windows. -->
					<argLine>@{surefireArgLine} -javaagent:${org.mockito:mockito-core:jar} -Dfile.encoding=UTF-8</argLine>
				</configuration>
			</plugin>

			<!-- Pokrycie testami: raport HTML/XML w target/site/jacoco (generowany w fazie verify) -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
						<configuration>
							<propertyName>surefireArgLine</propertyName>
						</configuration>
					</execution>
					<execution>
						<id>report</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<excludes>
						<!-- Klasy generowane / bez logiki - zaniżałyby statystykę bez powodu -->
						<exclude>**/FileTranslatorApplication.class</exclude>
						<exclude>**/dto/**</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!--
			Szybka pętla lokalna BEZ Dockera: ./mvnw test -Ph2

			Wyłącza kontenery (patrz TestInfrastructure) i zostawia konfigurację
			z application-test.yml, czyli H2 w trybie zgodności z PostgreSQL-em.

			CI tej ścieżki NIE używa i celowo: zielone -Ph2 nie jest obietnicą zielonego
			CI. H2 nie odwzorowuje migracji Liquibase na docelowym silniku, FOR UPDATE
			SKIP LOCKED, RedisBucketProvidera ani S3ObjectStore'a - a właśnie tam siedziały
			błędy, które ten projekt już raz złapał dopiero na PostgreSQL-u.
		-->
		<profile>
			<id>h2</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-surefire-plugin</artifactId>
						<configuration>
							<systemPropertyVariables>
								<test.database>h2</test.database>
							</systemPropertyVariables>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>