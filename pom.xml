<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.6</version>
		<relativePath/>
	</parent>
	<groupId>com.example</groupId>
	<artifactId>file_translator</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<properties>
		<java.version>21</java.version>
		<springdoc.version>3.0.0</springdoc.version>
		<bucket4j.version>8.14.0</bucket4j.version>
		<jacoco.version>0.8.13</jacoco.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<!-- Wypełniane przez jacoco:prepare-agent; pusta wartość domyślna sprawia,
		     że Surefire działa też wtedy, gdy JaCoCo nie zostało uruchomione. -->
		<surefireArgLine/>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<!-- Mail and template engine for transactional emails -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-thymeleaf</artifactId>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- MockMvc i @AutoConfigureMockMvc. W Boot 4 wydzielone ze spring-boot-starter-test
		     do osobnego startera dla webmvc. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- JWT library -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.12.6</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.12.6</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.12.6</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Migracje bazy danych.
		     UWAGA: w Spring Boot 4 autokonfiguracje zostały wyniesione z spring-boot-autoconfigure
		     do osobnych modułów. Sam liquibase-core NIE uruchamia migracji - potrzebny jest
		     dodatkowo moduł spring-boot-liquibase z LiquibaseAutoConfiguration. -->
		<dependency>
			<groupId>org.liquibase</groupId>
			<artifactId>liquibase-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-liquibase</artifactId>
		</dependency>

		<!-- Health checki, metryki, endpointy operacyjne -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Ograniczanie liczby żądań (token bucket). Wariant _jdk17 to build
		     skompilowany pod nowsze JDK, z tym samym API co bucket4j-core. -->
		<dependency>
			<groupId>com.bucket4j</groupId>
			<artifactId>bucket4j_jdk17-core</artifactId>
			<version>${bucket4j.version}</version>
		</dependency>
		<!-- Cache z ograniczonym rozmiarem i TTL - przechowuje kubełki limitów w pamięci.
		     Zwykła ConcurrentHashMap rosłaby w nieskończoność przy ataku z wielu adresów IP. -->
		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
		</dependency>
		<!-- Rozproszony magazyn limitów (opcjonalny, włączany przez app.rate-limit.store=redis).
		     Ciągnie za sobą klienta lettuce-core - świadomie BEZ spring-boot-starter-data-redis,
		     żeby Redis nie stał się zależnością startu aplikacji, dopóki nie jest używany. -->
		<dependency>
			<groupId>com.bucket4j</groupId>
			<artifactId>bucket4j_jdk17-lettuce</artifactId>
			<version>${bucket4j.version}</version>
		</dependency>
		<!-- Klient Redisa. Wersją zarządza BOM Spring Boota, więc bez podawania jej tutaj. -->
		<dependency>
			<groupId>io.lettuce</groupId>
			<artifactId>lettuce-core</artifactId>
		</dependency>

		<!-- Dokumentacja API generowana z kodu (OpenAPI 3 + Swagger UI) -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- Rozwiązuje wersję/ścieżkę do jara mockito-core na property,
			     żeby móc podać ją jako -javaagent w Surefire poniżej -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<id>getClasspathFilenames</id>
						<goals>
							<goal>properties</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<!-- @{surefireArgLine}: agent JaCoCo. Musi być tutaj, bo jawne <argLine>
					     nadpisuje to, co JaCoCo wstrzykuje domyślnie - klasyczna przyczyna
					     raportów pokrycia pokazujących 0%.
					     Składnia @{...}, a nie ${...}: Surefire rozwija @{...} dopiero
					     w momencie uruchomienia testów, czyli po tym, jak jacoco:prepare-agent
					     ustawi tę właściwość. ${...} zostałoby podstawione już przy wczytywaniu
					     POM-a - wtedy pustą wartością domyślną, i agent nigdy by nie wystartował.
					     -javaagent: rejestruje Mockito jako agenta JVM od razu przy starcie,
					     zamiast pozwalać mu dołączać się dynamicznie w locie (co JDK
					     będzie blokować w przyszłych wersjach - stąd ostrzeżenie w logach).
					     -Dfile.encoding=UTF-8: naprawia zniekształcone polskie znaki w logach na Windows. -->
					<argLine>@{surefireArgLine} -javaagent:${org.mockito:mockito-core:jar} -Dfile.encoding=UTF-8</argLine>
				</configuration>
			</plugin>

			<!-- Pokrycie testami: raport HTML/XML w target/site/jacoco (generowany w fazie verify) -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
						<configuration>
							<propertyName>surefireArgLine</propertyName>
						</configuration>
					</execution>
					<execution>
						<id>report</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<excludes>
						<!-- Klasy generowane / bez logiki - zaniżałyby statystykę bez powodu -->
						<exclude>**/FileTranslatorApplication.class</exclude>
						<exclude>**/dto/**</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>